{
  "name": "mcpbox-proxy",
  "version": "0.3.0",
  "description": "Cloudflare Worker proxy for MCPbox MCP gateway",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "0.8.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260719.1",
    "typescript": "^5.3.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.110.0"
  }
}
