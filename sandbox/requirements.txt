# Shared Sandbox Service
# Dynamically loads and executes MCP tools

httpx==0.28.1
fastapi==0.133.1
uvicorn==0.40.0
pydantic==2.12.5
cryptography==46.0.5
slowapi==0.1.9
regex==2026.2.28  # Drop-in replacement for re with timeout support (ReDoS protection)
