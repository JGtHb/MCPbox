# MCPbox Configuration
# Copy this file to .env and customize

# ============================================================
# Required Settings
# ============================================================

# Encryption key for credential storage (generate with: openssl rand -hex 32)
MCPBOX_ENCRYPTION_KEY=

# PostgreSQL password (use hex to avoid URL-parsing issues)
# Generate with: openssl rand -hex 16
POSTGRES_PASSWORD=

# Sandbox API key for backend-to-sandbox authentication
# Generate with: openssl rand -hex 32
SANDBOX_API_KEY=

# ============================================================
# Optional: Cloudflare Remote Access
# ============================================================
# Use the Cloudflare Setup Wizard at /tunnel/setup to configure.
# The wizard generates and stores all tokens (tunnel token, service token)
# in the database automatically. No environment variables needed.
# Run ./scripts/deploy-worker.sh --set-secrets to push tokens to the Worker.

# ============================================================
# Optional: Port Configuration
# ============================================================
# Setting these ports automatically configures BACKEND_URL, FRONTEND_URL,
# CORS_ORIGINS, VITE_API_URL, and the CSP header. You only need to set
# the URL variables below if you use a custom hostname (not localhost).

# Frontend port (default: 3000)
# MCPBOX_FRONTEND_PORT=3000

# Backend API port (default: 8000)
# MCPBOX_BACKEND_PORT=8000

# ============================================================
# Optional: Application Settings
# ============================================================

# Log level (default: INFO). Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# Log retention period in days (default: 30)
# LOG_RETENTION_DAYS=30

# Rate limit requests per minute (default: 100)
# RATE_LIMIT_REQUESTS_PER_MINUTE=100

# Backend URL — auto-derived from MCPBOX_BACKEND_PORT. Override for custom hostname.
# BACKEND_URL=http://localhost:8000

# Frontend URL — auto-derived from MCPBOX_FRONTEND_PORT. Override for custom hostname.
# FRONTEND_URL=http://localhost:3000

# CORS origins — auto-derived from MCPBOX_FRONTEND_PORT. Override for custom hostname.
# CORS_ORIGINS=http://localhost:3000

# CORS origins for MCP gateway (default: https://mcp.claude.ai,https://claude.ai)
# MCP_CORS_ORIGINS=https://mcp.claude.ai,https://claude.ai

# Enable Prometheus /metrics endpoint (default: true)
# ENABLE_METRICS=true

# Webhook URL for critical alerts — Discord, Slack, etc. (default: disabled)
# ALERT_WEBHOOK_URL=
